import { RealceDirective } from './realce.directive';

describe('RealceDirective', () => {
  it('should create an instance', () => {
    const directive = new RealceDirective();
    expect(directive).toBeTruthy();
  });
});
